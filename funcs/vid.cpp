#include "vid.h"

#include "mainwindow.h"



/*
  Timer = new QTimer();
  Timer->setInterval(250);
  connect(Timer, SIGNAL(timeout()), this, SLOT(CaptureScreenImage));

 */




void Videoshot::CaptureScreenImage()
{

}
